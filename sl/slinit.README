# These are the install instructions for the Scroll Lock Initiation script, which runs at root, before login

## Copy slinit to the startup directory
cp slinit /etc/init.d/

## Make the script executable

chmod ugo+x /etc/init.d/slinit

## Add the script to system startup

update-rc.d slinit defaults


