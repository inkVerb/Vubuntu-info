# Open Terminal
# Ctrl + Alt + T

# Root password
sudo passwd

# Basic updates
sudo apt-get update
sudo apt-get upgrade

# Canonical repo - the for Skype, adobe-flashplugin, and Microsoft fonts
sudo add-apt-repository "deb http://archive.canonical.com/ $(lsb_release -sc) partner"
sudo apt-get update
sudo apt-get install skype adobe-flashplugin

# Verbist (arts, code, email)
sudo apt-get install zip p7zip-full gzip guake mousepad kate dolphin thunar thunar-archive-plugin phatch thunar-media-tags-plugin vlc clementine amarok docky thunderbird firefox chromium-browser djview-plugin unity-chromium-extension midori epiphany-browser calligraauthor scribus sigil flowblade stellarium nautilus-dropbox grabc ubuntu-restricted-extras

# WebDev
sudo apt-get install git vim guake-indicator filezilla 

# Pre-Studio install (to answer the audio and MS font EULA responses before the long Ubuntu Studio install)
## "No" for jackd realtime control, EULA "Yes"
sudo apt-get install jackd2 msttcorefonts

# Ubuntu Studio (15-45 minutes for normal Internet connections)
sudo apt-get install ubuntustudio-audio ubuntustudio-audio-plugins ubuntustudio-graphics ubuntustudio-photography ubuntustudio-publishing linux-lowlatency ubuntustudio-video ubuntustudio-wallpapers

# Icons in GIMP, Inkscape, LibreOffice, etc (Run as user, not root)
gsettings set org.gnome.settings-daemon.plugins.xsettings overrides "{'Gtk/ShellShowsAppMenu': <0>, 'Gtk/ButtonImages': <1>, 'Gtk/MenuImages': <1>}"

# Remove title bar
sudo apt-get install maximus

# ...Ubuntu Unity for humans
sudo apt-get install unity-tweak-tool compizconfig-settings-manager


####### Kubuntu/Xubuntu etc #######
# ...For K/X_ubuntu
sudo apt-get install xubuntu-restricted-extras ubuntustudio-desktop ubuntustudio-menu
sudo apt-get install kubuntu-restricted-extras ubuntustudio-desktop ubuntustudio-menu


# ownCloud
mkdir Keys
cd Keys
rm *
sudo sh -c "echo 'deb http://download.opensuse.org/repositories/isv:/ownCloud:/desktop/Ubuntu_16.04/ /' >> /etc/apt/sources.list.d/owncloud-client.list"
sudo apt-get update
wget http://download.opensuse.org/repositories/isv:ownCloud:desktop/Ubuntu_16.04/Release.key
sudo apt-key add - < Release.key  
sudo apt-get install owncloud-client

# Really Slick Screensavers (No support in KDE 5/Kubuntu)
sudo apt-get install xscreensaver
sudo apt-get install rss-glx
## Check instructions in this file:
nano /usr/share/doc/rss-glx/README.xscreensaver

################ Extra Help ################
# fcitxc - Native Taiwan Chinese keyboard switcher, fcitx (and the others lang supp asks to add)
## AFTER install:
### Regional/language settings (let it update/fix language support)
#### > Keyboard input method: fctix
#### > Install/Remove Languages... (Chinese - Traditional)
#### * Check regional formats (lots of confusion: systems and novice programmers make assumptions about this)
### Add "fcitx" to starup programs (in addition to any fcitx-??? already in startup)
### Fcitx Configuration > Input Method > + (Add) > [uncheck Only Show Current Language] > Chewing (Chinese - Taiwan)
### Restart
sudo apt-get install fcitx fcitx-ui-qimpanel libreoffice-l10n-en-za fcitx-table-cangjie language-pack-zh-hant fonts-arphic-uming libreoffice-help-zh-tw libreoffice-l10n-zh-tw thunderbird-locale-en-gb fcitx-chewing fcitx-pinyin mythes-en-au fonts-arphic-ukai thunderbird-locale-zh-hant libreoffice-help-en-gb thunderbird-locale-zh-tw libreoffice-l10n-en-gb language-pack-gnome-zh-hant firefox-locale-zh-hant hunspell-en-ca 

# To install extra fonts
## put them in the .fonts folder of home directory
## refresh fonts:
fc-cache -f -v

# Add GNOME to Unity (May not work entirely)
sudo apt-get install gnome
## GNOME Desktop only
sudo apt-get install ubuntu-gnome-desktop
## GNOME Wayland session
sudo apt-get install gnome-session-wayland
## GNOME extra apps
sudo apt-get install gnome-music gnome-photos dia-gnome planner gromit

# Install QOwnNotes (markdown rendering)
sudo add-apt-repository ppa:pbek/qownnotes
sudo apt-get update
sudo apt-get install qownnotes

# Wacom tablet drivers (may not be necessary, try native Ubuntu drivers first)
sudo add-apt-repository ppa:doctormo/wacom-plus
sudo apt-get update
sudo apt-get dist-upgrade
## Then install the update kernel module
sudo apt-get install wacom-dkms


# Flash
## EASY: 32 bit, layer for 64 bit, native repository
sudo apt-get install flashplugin-installer

## Proper: 64 bit and 32 bit, partner repository (same as Skype)
sudo apt-get install adobe-flashplugin


# Spotify
## instructions to see if this is current: https://www.spotify.com/tw/download/linux/
sudo apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys D2C19886
sudo echo deb http://repository.spotify.com stable non-free | sudo tee /etc/apt/sources.list.d/spotify.list
sudo apt-get update
sudo apt-get install spotify-client

# evernote support - everpad
#### May be depreciated, if necessary remove ######
sudo add-apt-repository ppa:nvbn-rm/ppa
sudo apt-get update
## Remove if repo error:
# sudo add-apt-repository -r ppa:nvbn-rm/ppa
sudo apt-get install everpad

# To update to a new distro (use carefully, probably only on servers and preexisting minimal installations)
apt-get install update-manager-core
do-release-upgrade
